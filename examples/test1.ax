fn add(a: i64, b: i64) -> i64 {
    return a + b;
}

fn main() {
    let x: i64 = 10;
    let y: i64 = 20;
    let result: i64 = add(x, y);
    print(result);

    if x < y {
        print("x is less than y");
    } else {
        print("x is not less than y");
    }
}
